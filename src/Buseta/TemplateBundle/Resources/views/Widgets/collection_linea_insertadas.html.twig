{% block collection_widget %}
    {% spaceless %}
<div class="collection">
    {% if prototype is defined %}
        {% set attr = attr | merge({'data-prototype': block('collection_item_widget') }) %}
    {% endif %}
    {{ form_errors(form) }}

    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-body">
                    <div  {{ block('widget_container_attributes') }}>
                        <h4>Listado de líneas insertadas:</h4>

                        <div class="row form-group" >
                            <div class="col-lg-12">
                                <div class="col-lg-2" style="text-align: center;"><b>Número</b></div>
                                <div class="col-lg-2" style="text-align: center;"><b>Tipo</b></div>
                                <div class="col-lg-2" style="text-align: center;"><b>Condicion</b></div>

                                <div class="col-lg-2" style="text-align: center;"><b>Grupo</b></div>
                                <div class="col-lg-2" style="text-align: center;"><b>Subgrupo</b></div>
                                <div class="col-lg-2" style="text-align: center;"><b>Producto</b></div>
                                <div class="col-lg-2" style="text-align: center;"><b>Bodega</b></div>

                                <div class="col-lg-2" style="text-align: center;"><b>Cantidad Pedido</b></div>
                                <div class="col-lg-2" style="text-align: center;"><b>Precio</b></div>
                                <div class="col-lg-2" style="text-align: center;"><b>Impuesto</b></div>
                                <div class="col-lg-2" style="text-align: center;"><b>Monto</b></div>
                            </div>
                        </div>

                        <div id="tbody">
                            {% set counter = form | length %}
                            {% for rows in form %}
                                <div class="row form-group">
                                    {{ _self.template(rows) }}
                                </div>
                            {% endfor %}
                            {% if counter == 0 %}
                                <div id="no-elements-tr">No existen elementos. Presione "Adicionar" para agregar nuevos elementos.</div>
                            {% else %}
                                <div id="no-elements-tr">No existen elementos. Presione "Adicionar" para agregar nuevos elementos.</div>
                            {% endif %}
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>

    {{ form_rest(form) }}
    <div>

    </div>
    {% endspaceless %}
{% endblock collection_widget %}

{% block collection_item_widget %}
    {% spaceless %}
        {{ _self.template(prototype) }}
    {% endspaceless %}
{% endblock collection_item_widget %}

{% macro template(prototype) %}
    <div class="col-lg-2 form-group">
        {{ form_errors(prototype.numero) }}
        {{ form_widget(prototype.numero) }}
    </div>

    <div class="col-lg-2 form-group">
        {{ form_errors(prototype.tipo) }}
        {{ form_widget(prototype.tipo) }}
    </div>

    <div class="col-lg-2 form-group">
        {{ form_errors(prototype.condicion) }}
        {{ form_widget(prototype.condicion) }}
    </div>

    <div class="col-lg-2 form-group">
        {{ form_errors(prototype.grupos) }}
        {{ form_widget(prototype.grupos) }}
    </div>

    <div class="col-lg-2 form-group">
        {{ form_errors(prototype.subgrupos) }}
        {{ form_widget(prototype.subgrupos) }}
    </div>

    <div class="col-lg-2 form-group">
        {{ form_errors(prototype.productos) }}
        {{ form_widget(prototype.productos) }}
    </div>

    <div class="col-lg-2 form-group">
        {{ form_errors(prototype.bodegas) }}
        {{ form_widget(prototype.bodegas) }}
    </div>

    <div class="col-lg-2 form-group">
        {{ form_errors(prototype.cantidad_pedido) }}
        {{ form_widget(prototype.cantidad_pedido) }}
    </div>

    <div class="col-lg-2 form-group">
        {{ form_errors(prototype.precio_producto) }}
        {{ form_widget(prototype.precio_producto) }}
    </div>

    <div class="col-lg-2 form-group">
        {{ form_errors(prototype.impuesto) }}
        {{ form_widget(prototype.impuesto) }}
    </div>

    <div class="col-lg-2 form-group">
        {{ form_errors(prototype.monto) }}
        {{ form_widget(prototype.monto) }}
    </div>

    <div class="col-lg-1 form-group">
        <a class="btn btn-primary delete_tag_link" href="#"><i class="fa fa-minus-circle"></i></a>
    </div>

{% endmacro %}
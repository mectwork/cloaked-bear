<form action="{{ form.vars.action }}" method="POST" enctype="multipart/form-data" name="{{ form.vars.full_name }}" id="{{ form.vars.id }}">
    {% if form.vars.method == 'PUT' %}
        <input type="hidden" name="_method" value="PUT"/>
    {% endif %}

    {{ form_errors(form) }}
    {{ form_row(form.id) }}
    {{ form_row(form.tercero) }}
    {{ form_row(form._token) }}

    <div class="row">
        <div class="col-lg-12 form-group">
            {{ form_row(form.activo) }}
        </div>
    </div>

    {# buttons group #}
    <div class="row">
        <div class="col-md-4">
            <a class="btn btn-primary" id="btn_persona_save" href="#"><span class="glyphicon glyphicon-save"></span>
                {{ 'actions.save' | trans({}, 'BusetaBodegaBundle') }}</a>
            <a class="btn btn-default" id="btn_persona_cancel" href="{{ path('tercero') }}"><span class="glyphicon glyphicon-chevron-left"></span>
                {{ 'actions.cancel' | trans({}, 'BusetaBodegaBundle') }}</a>
        </div>
    </div>
</form>

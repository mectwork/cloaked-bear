<div class="modal" id="form_pedidocompralinea_delete_modal" tabindex="-1" role="dialog" aria-labelledby="deletePedidoCompraLinea" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4 class="modal-title" id="deletePedidoCompraLinea">{{ 'pedidocompralinea.modal.delete'|trans({},'BusetaBodegaBundle') }}</h4>
            </div>
            <div class="modal-body">
                <p>Usted está tratando de eliminar la línea <strong>"{{ entity.linea }}"</strong>.
                    Al aceptar la acción se eliminarán los datos de forma permanente. <strong>¿Está seguro que desea continuar?</strong></p>
                <form name="{{ form.vars.full_name }}" id="{{ form.vars.id }}" action="{{ form.vars.action }}"
                      method="post" enctype="multipart/form-data">
                    {% if form.vars.method == 'DELETE' %}
                        <input type="hidden" name="_method" value="DELETE"/>
                    {% endif %}
                    {{ form_widget(form) }}
                </form>
            </div>
            <div class="modal-footer">
                <a href="#" class="btn btn-default" id="btn_pedidocompralinea_cancel" data-dismiss="modal"><span class="glyphicon glyphicon-chevron-left"></span>
                    {{ 'actions.cancel' | trans({}, 'BusetaBodegaBundle') }}</a>
                <a href="#" class="btn btn-danger" id="btn_pedidocompralinea_delete"><span class="glyphicon glyphicon-trash"></span>
                    {{ 'actions.confirm' | trans({}, 'BusetaBodegaBundle') }}</a>
            </div>
        </div>
    </div>
</div>

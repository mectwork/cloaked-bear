{% extends "BusetaBodegaBundle::layout.html.twig" %}

{% block nombre_menu_superior %}

{% endblock %}

{% block titulo %}
    Listado de operaciones en almacenes
{% endblock %}

{% block titulo_encabezado %}
    Listado de operaciones en almacenes
{% endblock %}

{% block area_trabajo %}

    <table class="records_list table table-striped table-bordered table-condensed table-hover">
        <thead>
        <tr>
            <th>Fecha Compra</th>
            <th>Producto</th>
            <th>Cantidad de productos</th>
            <th>Almacén</th>
            <th>Realizado por</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.updated|date('d/m/Y') }}</td>
                <td>{{ entity.producto }}</td>
                <td>{{ entity.cantidadProductos }}</td>
                <td>{{ entity.almacen }}</td>
                <td>{{ entity.updatedBy }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="12">No se encontraron registros de operaciones en almacenes en el sistema.</td>
            </tr>
        {% endfor %}

        </tbody>
    </table>

    <div class="container">
        <div class="row">
            <div class="col-md-4 col-md-offset-4">
                {{ knp_pagination_render(entities) }}
            </div>
        </div>
    </div>

{% endblock %}
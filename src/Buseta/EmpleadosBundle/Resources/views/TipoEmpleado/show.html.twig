{% extends "BusetaEmpleadosBundle::layout.html.twig" %}

{% block title block('header') %}

    {% block header 'Ver Datos de Tipo de Empleado' %}

        {% block area_trabajo %}
            <table class="table table-striped" style="font-size: 16px;">
                <tbody>
                <tr>
                    <th>Nombre</th>
                    <td>{{ entity.nombre }}</td>
                </tr>
                <tr>
                    <th>Descripción</th>
                    <td>{{ entity.descripcion }}</td>
                </tr>
                </tbody>
            </table>

            <div class="form-actions">
                <a class="btn btn-primary" href="{{ path('tipoempleado_edit', { 'id': entity.id }) }}"> Editar</a>
                <a class="btn btn-primary" href="{{ path('tipoempleado') }}"> Listado</a>
                <a href="#delete" data-href="{{ path('tipoempleado_delete', {'id': entity.id}) }}" class="btn btn-danger">
                    <span class="glyphicon glyphicon-trash"></span> {{ 'Borrar' }}
                </a>
            </div>
        {% endblock %}
        {% block contenido %}

        {% endblock %}

        {% block javascripts %}
            {{ parent() }}
        {% endblock %}
